<%= render "components/card_componente",  element: recurrent_card, title: "Recurrent card", fields: ["id", "name", "board_id"]  %>

<% lists_table = render "components/table",  elements: @recurrent_card.labels, headers: ["name", "trello_id", "board_id"]  %>
<%= render "components/card_table",  table: lists_table, title: "Labels"  %>

<% lists_table = render "components/table",  elements: @recurrent_card.lists, headers: ["name", "trello_id", "board_id"]  %>
<%= render "components/card_table",  table: lists_table, title: "Lists"  %>